# Runtime dependencies (mirrored in pyproject.toml).
# Prefer: pip install -e ".[build]"
#
# System packages may still be required:
#   macOS: brew install enchant
#   Ubuntu/Debian: sudo apt-get install hunspell hunspell-en-us myspell-fa
#   Fedora: sudo dnf install hunspell hunspell-en hunspell-fa

PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0
pyenchant>=3.2.0
feedparser>=6.0.0
requests>=2.31.0
Markdown>=3.6
pyspellchecker>=0.7.2
